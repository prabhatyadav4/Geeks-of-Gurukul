#include <iostream> 
using namespace std; 

int main(){
    // Start of do-while loop
    do{
        cout<<"This loop is infinite"<<endl; // Print message to console
    }
    while(0); // Loop condition is false, so loop runs only once
    return 0; 
}